# Copy to .env.local for `vercel dev` — never commit real values.
# In production, set these in Vercel → Project → Settings → Environment Variables.

CONTACT_TO_EMAIL=
RESEND_API_KEY=
# Optional — must be a domain/sender you verify in Resend
# RESEND_FROM_EMAIL=Portfolio <hello@yourdomain.com>
